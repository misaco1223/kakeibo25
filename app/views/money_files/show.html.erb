<header class="mb-4 bg-neutral">
  <div class="navbar">
    <a href="#" class="btn btn-ghost text-xl"><%= @money_file.title %>の予算一覧</a>
    <p class="block"><%= @money_file.description %></p>
  </div>
</header>

<div class="navbar">
  <%= link_to new_budget_path, class: "btn btn-primary", method: :get do %>予算を新規作成<% end %>
  <%= link_to money_files_path(@money_file), class: "btn" do %>家計簿一覧へ戻る<% end %>
</div>

<br><br>
<%= render "budgets/index" %>